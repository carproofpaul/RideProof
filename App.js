import React from 'react';
import Home from './components/Home';

/**
 * RideProof
 * Entry point of the app, but all the content is in Home.
 */

export default class App extends React.Component {
  render() {
    return (
      <Home />
    );
  }
}
